# animation

纯前端 Canvas 动画示例：以手绘风格展示“资产缩水”场景——房屋与票据静置在右侧，红色价格曲线被“债务磁铁”一路拖拽下跌。

## 预览
直接打开 `index.html` 即可观看动画。页面内置播放/暂停、重播、速度调节控件。

如需本地服务器：
```bash
python -m http.server 8000
# 浏览器访问 http://localhost:8000
```
